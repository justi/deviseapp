<% if user_signed_in? %>
  <li>
    id <%= current_user.__id__ %>, email<%= current_user.email %><br>
    Session data <%= user_session  %> <br>

  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>
  </li>
<% end %>